# Autonomous Research Mini-Pipeline (Streamlit)

A 20-minute research sprint assistant:
Hypothesis → Literature Compression → Attack → Minimal Experiment → Conclusion template.

## Run locally
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
